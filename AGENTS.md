# AGENTS.md

本文件放置于海豚云仓库根目录，适用于整个仓库。开发者 A、开发者 B 与质量构建负责人 C 的所有 Codex 会话，在修改代码前必须先阅读本文件及当前 Issue 引用的文档。

## 1. 项目目标

海豚云是一套 React Native + Expo 校园应用，用一套工程提供教师端、班级端、家庭端、银行端、自治会端和管理端，并交付 Web、Android APK、iOS 本地测试版和 Windows EXE。

产品包含三套相互独立的体系：

1. 学生分：教师端和授权班级端管理。
2. 班级分：自治会端管理。
3. 海豚币：银行端管理；教师只能按规则发起奖励或罚款。

## 2. 绝对边界

- 开发过程只能由开发者、Codex、Git 和 GitHub 完成。
- 禁止使用 Coze、Coze CLI 或其他外部生成器创建、修改、调试、审查或部署本项目代码。
- Coze 仅作为应用运行时依赖，通过后端受控 API 接入 AI 中心。
- 禁止让 Coze 直接访问数据库、服务角色密钥或用户 JWT。
- Coze 不可用时，课件、学生分、班级分、作业、成绩、海豚币和撤销功能必须继续可用。
- 不接入真实货币、真实支付和自动作出学生好坏评价的 AI。
- 不在客户端、日志、Issue、PR 或截图中提交任何密钥和真实学生隐私数据。

## 3. 开始工作前

Codex 必须：

1. 读取当前 GitHub Issue、验收标准和关联文档。
2. 检查当前分支、工作区状态和最近提交。
3. 确认任务只涉及一个明确范围。
4. 找到将被修改文件的现有实现、测试和调用关系。
5. 如果需要改变公共接口、数据库表、权限或跨模块结构，先提出 ADR 或契约修改。

不得在需求不清楚时自行增加页面、角色、业务流程或第三方依赖。

## 4. 工程规则

- 使用 TypeScript 严格模式，不使用无说明的 `any`。
- 使用 `pnpm` 管理依赖和 workspace。
- Expo 客户端负责展示和交互；权限判断、账务写入与审计必须在服务端再次执行。
- 公共类型和验证规则放在共享 package，不复制同一业务类型。
- 所有时间存储为 UTC，界面按用户时区展示。
- 所有金额、分值使用整数；海豚币不得用浮点数。
- 客户端公开环境变量只能使用 `EXPO_PUBLIC_` 前缀；任何服务端密钥禁止使用该前缀。
- 新依赖必须说明用途、维护状态、包体积或安全影响。
- 不进行与当前 Issue 无关的全仓库重构或格式化。

## 5. 数据与业务不变量

- 学生分、班级分、海豚币和成绩是四类不同数据，不得共用余额字段。
- 学生分不自动兑换海豚币；班级分不分摊给个人；成绩不计入学生分。
- 海豚币余额不得低于零；余额不足时罚款单进入待处理状态。
- 分值和海豚币变更采用不可变流水，禁止直接删除或覆写历史记录。
- 撤销通过新增反向记录完成，必须引用原操作 ID、记录原因和操作者。
- 同一操作幂等键只能成功一次。
- 班级公共端不得展示个人成绩、海豚币余额和罚款详情。
- 家庭端只能查看当前用户或已绑定学生的数据。
- 所有高风险写操作必须经过服务端授权、确认和审计。

## 6. Git 规则

- 禁止直接提交或推送到 `main`。
- 分支从最新 `origin/main` 创建，名称格式见 `docs/04-GitHub协作规范.md`。
- 一个 Issue 对应一个分支和一个 PR。
- 提交遵循 Conventional Commits，例如 `feat(score): add class ranking`。
- 不改写其他开发者已推送分支的历史。
- 不提交 `.env`、构建产物、个人 IDE 配置、真实数据或临时调试文件。
- Codex 只有在开发者明确要求时才执行 commit、push、merge、tag 或发布。
- 发生冲突时先理解双方意图，不使用破坏性命令覆盖他人修改。
- A、B 的功能 PR 由 C 负责主审；C 提交的测试或修复 PR 必须由 A 或 B 复核，禁止自审自合并。

## 7. 完成条件

Codex 在声称任务完成前必须：

1. 执行与修改范围相关的 lint、类型检查和测试。
2. 对权限、账务、撤销或迁移变更运行相应集成测试。
3. 检查 `git diff`，确认没有无关修改和密钥。
4. 更新受影响的文档、API 契约和测试。
5. 输出修改摘要、验证命令、验证结果和仍未验证的边界。
6. 使用 PR 模板准备交付说明，但不得自动合并。

“可以编译”不等于业务完成；涉及界面时至少验证目标角色的真实操作路径。

## 8. 代码审查重点

审查按以下优先级：

1. 越权访问、密钥泄漏、账务错误、数据丢失。
2. 撤销、幂等、并发和迁移回退问题。
3. 是否破坏其他端或离线于 Coze 的普通功能。
4. 业务验收标准和错误处理。
5. 可维护性、测试质量和界面一致性。

审查意见必须指出文件、具体问题、触发条件和建议修复方向，不输出笼统评价。

# Codex 提示词库

本文件供开发者 A、开发者 B 和质量构建负责人 C 使用。复制提示词时替换 `<...>` 占位符，并附上 GitHub Issue 链接或完整内容。

## 0. 使用规则

每次 Codex 会话只处理一个 Issue。提示词必须至少包含：

- 当前身份：A、B 或 C。
- 当前分支和 Issue。
- 目标结果与验收标准。
- 允许修改和禁止修改的路径。
- 必须运行的验证。
- 是否允许 commit、push、创建 PR、tag 或构建。

默认不授权 Codex commit、push、merge、tag、发布和外部部署。`START_HERE.md` 的实时协作提示词是例外：它明确授权当前 Issue 功能分支的 checkpoint commit、push 和 Draft PR，但不授权 main、force push、merge、tag、发布或部署。

任何提示词都继承以下硬规则：

```text
先完整阅读仓库根目录 AGENTS.md 和 Issue 引用文档。
涉及页面、业务、接口或数据时，必须阅读 docs/07-产品语言与视觉规范.md 和 docs/08-代码与资源命名规范.md；禁止自行创建同义名称、状态、图标或颜色。
开发过程可以使用 Coze 对话以及 Agent、Skill、Workflow 能力辅助需求拆解、方案验证和内容整理；任何代码改动仍必须进入 Git/GitHub 并通过测试与审查。
Coze 不是应用运行时依赖；产品 AI 由海豚云服务端 AI Gateway 接入 DeepSeek。不要向任何 AI 工具提交密钥、真实学生数据或生产数据库内容。
保留用户和其他开发者已有修改，不做 Issue 外重构。
不得输出、读取或提交密钥与真实学生数据。
完成前运行相关验证，检查 git diff，并报告未验证边界。
```

---

## 1. 新会话初始化提示词

适合三人每天开始新任务时使用。

```text
你正在海豚云仓库中工作，我的角色是【开发者 A / 开发者 B / 质量构建负责人 C】。

当前任务：GitHub Issue #<编号>《<标题>》
当前分支：<分支名>
Issue 内容与验收标准：
<粘贴 Issue 或提供可读取链接>

先执行只读检查：
1. 完整阅读 AGENTS.md，以及 Issue 引用的 docs 文档。
2. 检查 git status、当前分支、最近提交和相关文件。
3. 找出现有实现、调用关系、测试和可能的跨模块影响。
4. 用简短计划列出要修改的文件、验证方式和风险。

边界：
- 只处理这个 Issue，不顺便重构无关代码。
- 可以使用 Coze 辅助需求拆解和方案验证，但不得上传密钥、真实学生数据或生产配置；正式结论必须由仓库代码和测试证明。
- 暂不 commit、push、merge、tag 或发布，除非我之后明确授权。
- 如果验收标准与现有文档、接口、权限或数据不变量冲突，先指出证据并暂停会改变方向的实现。

检查完成后直接开始可以安全确定的实现，并持续报告关键进展。
```

---

## 2. 开发者 A：客户端与教学功能提示词

```text
我是海豚云开发者 A。请实现 Issue #<编号>：<课件 / 作业 / 成绩 / 六端页面 / 共享 UI 功能>。

目标结果：
<一句话描述用户最终可以完成什么>

验收标准：
<逐条粘贴 Given/When/Then>

允许修改：
- apps/client/app/<范围>
- apps/client/src/features/<范围>
- apps/client/src/shared/<范围>
- packages/ui、packages/auth、packages/api-client 中与本 Issue 直接相关文件
- 对应测试和文档

需要先协调、不要自行改变：
- supabase/migrations 和公共数据库契约由开发者 B 主持
- packages/domain、validation 的公共类型变更先说明影响
- 不修改学生分、班级分、币账和撤销算法，除非 Issue 明确要求

实现要求：
- 业务逻辑放在 feature，不堆在 Expo Router 路由文件。
- 提供加载、空、错误、无权限和成功状态。
- 客户端权限只隐藏入口，不能代替服务端授权。
- 班级公共端不得展示成绩、余额或罚款详情。
- Web 和原生端共用业务逻辑，对文件选择／打开差异做适配。
- DeepSeek 不可用时普通页面必须完整可用；Coze 只用于开发辅助，不参与产品运行时请求。

验证：
- 运行 lint、typecheck、相关单元／组件测试。
- 实际走通 <教师/班级/家庭> 角色路径。
- UI 变化生成可供 PR 使用的截图说明。
- 最后检查 git diff，报告修改、命令结果、风险和未验证平台。

不要自动 commit 或 push。
```

---

## 3. 开发者 B：服务端与治理功能提示词

```text
我是海豚云开发者 B。请实现 Issue #<编号>：<学生分 / 班级分 / 银行 / 罚款 / 撤销 / AI 网关功能>。

目标与验收：
<粘贴目标和验收标准>

允许修改：
- apps/client/src/features/<本 Issue 业务域>
- supabase/functions/<本 Issue 范围>
- supabase/migrations 新增迁移，不修改已合并迁移
- supabase/tests/<本 Issue 范围>
- packages/domain、validation、api-client 中直接相关契约
- 对应文档与测试

实现前必须确认：
- 角色授权范围和 RLS 策略。
- 是否需要事务、行锁、幂等键、operation_id 和审计。
- 撤销时原记录如何保留、反向记录如何关联。
- API 变化是否会影响开发者 A 正在使用的 DTO。

硬性不变量：
- 学生分、班级分、海豚币、成绩不得混用字段或互相自动换算。
- 海豚币余额不得为负；余额和流水必须在同一事务提交。
- 不信任客户端提供的 actor、role 和作用范围。
- 分值和币账流水不可变；撤销新增反向记录。
- 相同幂等键不得重复产生业务结果。
- 可使用 Coze 辅助梳理事务、权限和异常场景，但不得向其提交真实数据或密钥。
- 若涉及产品 AI，只实现海豚云 AI Gateway、DeepSeek 适配器和白名单意图接口；DeepSeek 不能直连数据库或执行未确认写操作。

验证：
- lint、typecheck、单元测试。
- 本地迁移从空库执行。
- RLS 正向与反向角色测试。
- 幂等、并发、余额不足和重复撤销测试。
- 检查 git diff 和迁移回退说明。

最后输出变更摘要、验证证据、API／迁移影响、风险和未验证边界。不要自动 commit 或 push。
```

---

## 4. 契约先行提示词

用于 A、B 并行前先固定 DTO、Schema 和 mock。

```text
请为 Issue #<编号>制作一个“只包含契约”的小型变更，供开发者 A 和 B 并行。

业务场景：<说明>
调用方：<页面/feature>
服务端：<函数/API>

只允许修改：
- packages/domain
- packages/validation
- packages/api-client 的接口定义或 mock
- docs/03-API与AI接入.md 或仓库对应 API 文档
- 契约测试

不要实现完整 UI、数据库逻辑或真实 DeepSeek 调用。

输出必须包含：
- 请求、成功响应和错误响应 Schema。
- 角色、范围、幂等和审计要求。
- 至少一个正常、一个无权限、一个验证失败示例。
- 向后兼容性说明。
- A、B 各自后续实现清单。

先阅读 AGENTS.md。可以使用 Coze 辅助检查契约完整性，但正式产物必须落入仓库并通过类型检查和契约测试。不要自动 commit 或 push。
```

---

## 5. 数据库迁移与 RLS 提示词

```text
请针对 Issue #<编号>实现数据库迁移和 RLS。

当前实体与关系：<粘贴或引用文档>
角色矩阵：<粘贴相关权限>
验收标准：<粘贴>

要求：
1. 只新增一个命名为 YYYYMMDDHHMM_<issue>_<description>.sql 的迁移；不编辑已合并迁移。
2. 使用 UUID、UTC timestamptz、必要外键、唯一约束和索引。
3. 明确 SELECT/INSERT/UPDATE/DELETE 策略；不可变流水禁止 UPDATE/DELETE。
4. 不信任客户端 actor_id 和 role，使用 auth.uid() 及授权关系。
5. 为每条策略写正向和越权测试。
6. 涉及余额、撤销或幂等时使用原子函数、事务约束和并发测试。
7. 写出前向迁移、兼容性和回退方案；若回退会丢数据必须明确说明。

请先检查现有 Schema 和迁移顺序，避免重复表、策略或索引。可使用 Coze 辅助推演角色场景，但不得上传真实数据、连接串或密钥。执行空库迁移和数据库测试，最后报告结果，不要自动 commit 或 push。
```

---

## 6. 质量负责人 C：代码 Review 提示词

```text
我是海豚云质量与构建负责人 C。请 Review PR #<编号>，当前只做只读审查和验证，不修改代码。

请先阅读：
- AGENTS.md
- 关联 Issue 和验收标准
- PR 描述、diff、相关文档与测试

审查优先级：
1. P0：越权、密钥泄露、余额错误、数据损坏、错误迁移。
2. P1：核心流程失败、幂等失效、并发撤销、DeepSeek 故障拖垮普通功能。
3. 验收标准、错误处理、跨端行为和回归。
4. 测试质量、可维护性和无关改动。

涉及业务时重点检查：
- 学生分、班级分、海豚币和成绩是否独立。
- RLS 是否使用真实登录身份而非客户端角色。
- 币账是否原子、余额是否可能为负。
- 撤销是否新增反向记录并防止重复。
- 班级端是否泄露成绩、余额或罚款。
- AI 写操作是否只生成草稿、确认后由海豚云服务执行。
- Coze 辅助结果不能替代 diff、测试和运行证据；产品代码不得包含 Coze 运行时依赖或开发凭据。

运行 PR 相关的 lint、typecheck、测试和最短角色路径。不要因为 CI 绿就默认正确。

输出格式：
- Findings：按 P0/P1/P2 排序；每条包含文件与行、问题、触发条件、影响和修复方向。
- Questions：确实需要作者回答的项目。
- Verification：实际执行命令、commit SHA 和结果。
- Verdict：Approve / Request changes / Blocked，并说明未验证边界。

如果没有可操作问题，明确写“未发现阻止合并的问题”，同时说明剩余风险。不要修改、commit、push 或合并。
```

---

## 7. C：缺陷复现与修复提示词

```text
我是质量与构建负责人 C。请诊断并修复 Issue #<编号>。

缺陷：<现象>
环境和 commit：<信息>
复现步骤：<步骤>
期望／实际：<结果>

先只读定位根因：检查相关代码、日志、测试和最近变更，实际复现后给出证据。不要在尚未确认根因时大范围修改。

修复边界：
- 允许修改 <路径>。
- 禁止做无关重构和依赖升级。
- 先添加能在修复前失败的回归测试，或说明为什么无法自动化。
- 若修复会改变 API、数据库或业务规则，先停止并请求对应开发者确认。
- 可使用 Coze 辅助复现思路整理，但根因、补丁和验证必须来自当前仓库与实际环境。

完成后运行定向测试、受影响角色路径、lint 和 typecheck。检查 git diff，输出根因、修复、验证和剩余风险。不要自动 commit 或 push；修复 PR 后续必须由 A 或 B Review。
```

---

## 8. C：完整回归提示词

```text
我是质量负责人 C。请在 commit <SHA> 上执行海豚云 RC 回归，只进行安全的测试和诊断，不修改业务代码。

环境：<local/staging>
目标版本：<tag/sha>
演示账号来源：<只说明位置，不粘贴密码>

按 docs/06-测试发布与应急.md 执行：
1. 安装锁定依赖并运行 lint、typecheck、单元测试、数据库/RLS 测试。
2. 从空库执行迁移并加载合成种子。
3. 逐项完成八条核心演示路径。
4. 专测越权、重复请求、余额不足、并发撤销和 DeepSeek 关闭降级。
5. 记录每个平台、角色、步骤、结果、request_id/operation_id 和脱敏证据。

发现问题时先建立缺陷清单，不直接混合修复。按 P0/P1/P2 分类并给出最小复现。

最终输出：
- 测试环境和 commit。
- 通过／失败／未测统计。
- 缺陷列表。
- 是否允许进入 RC 构建。
- 未验证边界。

Coze 可用于整理回归步骤，但测试结果只能来自实际命令与环境；运行时 DeepSeek 作为被测外部模型服务。
```

---

## 9. C：Web 构建提示词

```text
我是质量与构建负责人 C。请从已验收 tag <TAG> 构建海豚云 Web 交付物。

授权范围：
- 可以执行干净构建、打包、校验和必要的构建配置修复。
- 不允许修改业务功能、绕过测试、使用本地未提交代码或重新生成产品代码。
- Coze 可用于整理构建故障信息，但正式构建只能使用锁定仓库、依赖和 CI；产品运行时模型服务为 DeepSeek。
- 未经我明确确认，不上传外部平台、不发布 GitHub Release、不创建 tag。

步骤：
1. 验证工作区干净、tag 指向的 commit 和 CI 状态。
2. 使用锁定的 Node 与 pnpm 版本。
3. 构建 Web 静态产物和 Docker/Nginx 生产镜像。
4. 对桌面、平板和 390px 手机 Web 执行主演示冒烟。
5. 检查客户端产物不含 DeepSeek API Key、Coze 开发凭据、服务角色密钥和真实数据。
6. 生成 SHA-256、构建环境、耗时和日志位置。
7. 按规定命名构建物并准备 release notes 草稿。

任何端失败时先保存完整错误和环境，判断是代码、配置、签名还是平台限制；不要隐藏失败，也不要把未验证文件标为成功。

最终输出构建矩阵：Web 文件／镜像、版本、SHA-256、启动、核心冒烟和已知问题。
```

---

## 10. DeepSeek 运行时接入提示词

仅开发者 B 在对应 Issue 中使用。Coze 可以辅助拆解意图、Agent／Skill／Workflow 和降级场景；产品运行时实现必须是海豚云服务端对 DeepSeek 的受控适配。

```text
请在海豚云本地代码中实现 Issue #<编号> 的 DeepSeek 运行时接入。可以使用 Coze 辅助推演场景和白名单意图，但不得上传密钥、真实数据或生产配置；最终适配代码和测试必须提交到当前 GitHub 分支。

目标：<只读查询 / 写操作草稿 / 降级>
允许的意图／操作代码：<白名单>
角色范围：<矩阵>

安全要求：
- DeepSeek API Key 只存在服务端环境。
- DeepSeek 不接收 Supabase service role key 或用户 JWT。
- 使用短期、单用途、绑定用户和工具白名单的上下文 token。
- DeepSeek 只返回文本、白名单意图或 action draft。
- 写操作不得由 DeepSeek 直接执行；客户端确认后由海豚云服务重新授权、检查版本并幂等执行。
- 无效结构、超时、限流和服务故障统一降级，不影响普通业务。
- 日志脱敏并保留 request_id，不记录完整隐私内容。

测试：
- mock DeepSeek 正常、超时、无效 JSON、越权意图、重复响应。
- 草稿过期、重复确认、权限变化和数据版本变化。
- DeepSeek 完全关闭时普通业务回归。

不要在测试中调用真实 DeepSeek，除非 Issue 明确要求在隔离 staging 做运行时联调且我另行提供授权。不要 commit 或 push。
```

---

## 11. 安全审查提示词

```text
请对 Issue/PR #<编号> 做海豚云安全审查，只读，不修改代码。

检查范围：<路径>
重点资产：学生身份、家庭绑定、成绩、分数、海豚币、罚款、文件、DeepSeek API Key 与 Coze 开发凭据。

请建立简短威胁模型：攻击者、入口、资产、信任边界。逐项检查：
- 横向和纵向越权。
- 客户端伪造 role、actor、class_id、student_id。
- RLS 漏洞和 service role 滥用。
- 文件类型、路径、签名 URL 和公共端泄露。
- 幂等、并发、负余额和双重撤销。
- 日志、错误、bundle、CI 和截图中的密钥或隐私。
- 模型提示注入、任意意图代码、越权上下文和未经确认写入。

输出按 P0/P1/P2 排序的可操作发现，每条给出文件行、利用前提、影响和最小修复。区分已验证漏洞、合理风险和需要动态验证的假设。不要调用真实 DeepSeek 或上传敏感数据，不要修改、commit 或 push。
```

---

## 12. PR 交付准备提示词

```text
当前 Issue #<编号> 的实现已完成。请做提交前自检，但不要 commit 或 push。

1. 重新读取 Issue 验收标准和 AGENTS.md。
2. 检查 git status、完整 diff 和新增文件。
3. 找出无关修改、调试代码、临时文件、密钥、真实数据和遗漏文档。
4. 运行本 Issue 需要的 lint、typecheck、测试、数据库或角色路径。
5. 根据 .github/pull_request_template.md 生成 PR 描述草稿。
6. 建议 1—3 个 Conventional Commit；如果当前 diff 应拆 PR，请先指出。

输出：
- 变更结果。
- 文件级摘要。
- 验收标准对应证据。
- 实际命令与结果。
- 风险、迁移、回退和未验证边界。
- PR 标题与完整描述草稿。

可使用 Coze 辅助整理自检清单，但不得上传密钥、真实数据或生产配置。不要执行 git add、commit、push、merge 或发布。
```

---

## 13. 授权 Codex 提交和推送提示词

只在开发者检查完自检结果后使用。

```text
我已经确认当前 diff 和 PR 草稿。现在授权你：
1. 只暂存以下文件：<明确列表>。
2. 再次显示 staged diff 摘要并检查密钥。
3. 使用提交信息 `<type>(<scope>): <summary>` 创建提交。
4. 推送到当前分支 `<branch>`。

不允许修改文件、不允许 force push、不允许推送 main、不允许合并、不允许创建 tag 或发布。如果暂存内容与文件列表不一致，立即停止并报告。
```

---

## 14. 每日状态整理提示词

```text
请根据当前 Git 状态、GitHub Issue/PR 和今天的验证记录，生成海豚云每日同步摘要。不要修改文件或外部状态。

格式严格为：
Yesterday：已完成事项与 PR 链接
Today：唯一 In Progress Issue 和预期交付
Blocked：阻塞事项、责任人、所需决定
Risk：对 RC、数据、安全或构建的影响
Verification：已执行测试与结果

不要把未合并代码写成“已完成”，不要隐藏未验证平台。
```

---

## 15. ADR 决策提示词

```text
当前 Issue 需要一个会影响多人的技术决策：<决策主题>。

请只做分析并根据 docs/ADR模板.md 生成 ADR 草稿，不修改代码。

背景：<现状和约束>
必须满足：<业务、安全、工期约束>
候选方案：<已有候选，没有可留空>

请比较每个方案的复杂度、两周交付影响、Web 兼容、数据迁移、安全、测试和回退。给出明确推荐及不选择其他方案的具体原因。可以使用 Coze 辅助分析候选方案，但产品运行时 AI 边界以 DeepSeek + 海豚云 AI Gateway 为准。
```

## 16. 提示词反例

不要使用：

```text
帮我把海豚云做完。
看看代码有没有问题并修好。
把所有测试都跑一下然后发布。
```

这些提示缺少 Issue、范围、验收、权限和验证边界，容易造成跨模块修改、误提交或假完成。应使用前面的模板，把任务缩小到可在一个 PR 内完成。
